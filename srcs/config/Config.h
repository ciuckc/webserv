#pragma once

#include <cstddef>
#include <memory>
#include <vector>

class Config {
 public:
  Config() = default;
  Config(const Config& rhs) = default;
  Config& operator=(const Config& rhs) = default;
  ~Config() = default;

 private:
};
